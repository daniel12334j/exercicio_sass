import { FormHero, FormDiv, HeroTitle } from './HeroTs'

const Hero = () => (
  <FormHero>
    <FormDiv className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormDiv>
  </FormHero>
)

export default Hero
